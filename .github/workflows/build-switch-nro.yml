name: Build Switch NRO

on:
  push:
    branches: [main]
    paths:
      - "wireless-sync-hub/switch/**"
      - ".github/workflows/build-switch-nro.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkita64:latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build NRO
        working-directory: wireless-sync-hub/switch
        run: make

      - name: Upload NRO artifact
        uses: actions/upload-artifact@v4
        with:
          name: NX-Save-Sync-Hub-NRO
          path: wireless-sync-hub/switch/NX-Save-Sync.nro

      - name: Attach NRO to nightly release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'push'
        with:
          tag_name: nro-${{ github.run_number }}
          name: Switch NRO build ${{ github.run_number }}
          draft: false
          prerelease: true
          files: wireless-sync-hub/switch/NX-Save-Sync.nro
